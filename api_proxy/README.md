# api_proxy

Api para validar y procesar textos transformados a audio.

# Instalar solo dependencias principales
pip install -e .

# Instalar con dependencias de test
pip install -e ".[test]"

# Instalar con todas las herramientas de desarrollo
pip install -e ".[dev]"

# Generar requirements.txt si alguien lo necesita (legacy)
pip freeze > requirements.txt
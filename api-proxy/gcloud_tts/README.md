# gcloud-tts

Cliente Python para Text-to-Speech de Google Cloud.

## Instalación

```bash
# Instalar desde PyPI (cuando lo publiques)
pip install gcloud-tts

# Instalar desde repositorio
pip install git+https://github.com/brianmalavenda/gcloud-tts.git

# Instalar en modo desarrollo
git clone https://github.com/brianmalavenda/gcloud-tts.git
cd gcloud-tts
pip install -e .